---
allowed-tools: Bash(git:*)
description: Checkout main, fetch, and pull latest changes
model: "haiku"
---

Sync with the main branch by checking out main, fetching from origin, and pulling the latest changes.

Run these git commands in sequence:
1. `git checkout main`
2. `git fetch origin`
3. `git pull`

Report the results of each command to the user.
