---
description: Map source directory structure to docs sections
argument-hint: [source_path]
allowed-tools: Read, Write, Edit, Glob, Grep, Bash(eza:*), Bash(tree:*), Bash(mkdir:*)
---

# Docs Scaffold

Map source directory structure to `docs/10-codebase/` sections.

**Target**: $ARGUMENTS (if not provided, ask which directory to scaffold)

## Instructions

Read and execute: `.claude/skills/docs-framework/30-workflows/20-scaffold.md`
